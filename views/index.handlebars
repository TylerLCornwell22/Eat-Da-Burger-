<div class="text">
<h1>Eat Da Burger</h1>

<h2>Burgers to eat!</h2>

<ul class="list">
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burgers that need to be eaten!</h2>

<ul class="list">
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    {{!-- <label for="devoured">Devour</label><br> --}}
    <input type="radio" name="devoured" value="1" checked> Devour<br>
    <input type="radio" name="devoured" value="0"> eaten
  </div>

  <button type="submit"id="sumbit">Add burger</button>
</form>
</div>